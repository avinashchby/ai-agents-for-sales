{
  "title": "Ai Agents For Sales Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "mode": {
      "title": "Mode",
      "type": "string",
      "description": "compare-tools: Returns list of AI tools in this category with ratings and pricing. extract-data: Returns structured data for AI agent pipelines.",
      "enum": [
        "compare-tools",
        "extract-data"
      ],
      "default": "compare-tools",
      "editor": "select"
    },
    "maxResults": {
      "title": "Max Results",
      "type": "integer",
      "description": "Maximum number of results to return.",
      "default": 25,
      "minimum": 1,
      "maximum": 200,
      "editor": "number",
      "sectionCaption": "General Settings"
    }
  },
  "required": [
    "mode"
  ]
}
